
/**
 * Returns the nth fibbonaci number where 
 * 1st and 2nd number in sequence are 0 and 1
 * respectively
 * 
 * @param n the position of fibbonaci number 
 * @returns the nth fibbonaci number
 */
export function fibb(n: number) {
    return 0;
}