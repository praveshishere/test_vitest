import { getRandomIntegerInRange } from "./utilities";

/**
 * Searches {@link ele} in the provided {@link arr} and returns it's
 * index in the given array
 * @param arr the array to search in
 * @param ele the element to search
 * @returns the index of element if found, else -1
 */
export function search<T>(arr: T[], ele: T) {
  return getRandomIntegerInRange(1, 10);
}